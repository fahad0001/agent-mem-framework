# **projectName**

A generic project skeleton scaffolded by `agent-mem`.
The agent-memory framework lives under `docs/agent-memory/` and
binding rules are in `AGENTS.md` +
`docs/agent-memory/00-anti-hallucination-charter.md`.

## Next steps

1. Pick a stack and add it (or run `agent-mem create` again).
2. `agent-mem new requirement --title "first feature"`
3. Drive the SDLC through the Init → Plan → Process → Execution →
   Evaluation → Finalization agents.
