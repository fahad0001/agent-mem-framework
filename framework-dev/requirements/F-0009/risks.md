# Risks - F-0009

- RISK-1: AHC block extraction can select marker examples from prose if the extractor uses the first marker pair.
- RISK-2: Vendor renderers can emit non-selected agents unless filtering happens before render or cleanup covers vendor-specific paths.
- RISK-3: Empty directories are not reliable package artifacts unless they contain a file such as `README.md`.
