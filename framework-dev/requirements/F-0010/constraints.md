# Constraints - F-0010

- Do not add dependencies without an ADR.
- Preserve existing skill IDs and user-facing trigger descriptions.
- Keep canonical skills in `.github/skills/` and sync them into `cli/ai-sdlc/templates/framework/.github/skills/`.
- Generated projects must remain usable without package-manager installs beyond their own existing setup.
