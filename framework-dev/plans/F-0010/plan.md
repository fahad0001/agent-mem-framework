# Plan - F-0010

Create an automation-first skill package structure and migrate the six shipped skills to it. Add a deterministic validator and wire it into repository checks. Sync the framework templates so generated projects receive the same structure.
