# Rollback Plan - F-0011

- Remove skill manifests and research validation from `skill-guard.mjs`.
- Remove generated folder indexes and `folderIndexes` schema additions.
- Remove `build:memory-indexes` and `validate:memory-indexes` package scripts.
- Re-run framework sync and validation.
